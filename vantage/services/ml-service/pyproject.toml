[project]
name = "vantage-ml-service"
version = "0.1.0"
description = "Vantage XGBoost adjustment layer with SHAP explanations"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
